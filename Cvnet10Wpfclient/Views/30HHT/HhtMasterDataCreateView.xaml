<helpers:BaseWindow x:Class="Cvnet10Wpfclient.Views._30HHT.HhtMasterDataCreateView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:Cvnet10Wpfclient.Helpers"
	xmlns:local="clr-namespace:Cvnet10Wpfclient.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="clr-namespace:Cvnet10Wpfclient.ViewModels._30HHT"
	Title="HHT用マスタデータ作成(cvnetcom)"
	Width="600" Height="400"
	Background="{DynamicResource AppCommonBackgroundBrush}"
	FocusManager.FocusedElement="{Binding ElementName=OutputPathTextBox}"
	Foreground="{DynamicResource MaterialDesignBody}"
	ResizeMode="NoResize"
	TextOptions.TextFormattingMode="Display"
	mc:Ignorable="d">
	<helpers:BaseWindow.DataContext>
		<vm:HhtMasterDataCreateViewModel />
	</helpers:BaseWindow.DataContext>
	<Grid Margin="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock Margin="0,0,0,20"
			FontWeight="SemiBold"
			Style="{StaticResource MaterialDesignHeadline6TextBlock}"
			Text="HHT用マスタデータを作成します" />

		<materialDesign:Card Grid.Row="1" Padding="24">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="24" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="110" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<TextBlock Grid.Row="0"
					Grid.Column="0"
					VerticalAlignment="Center"
					FontSize="15"
					FontWeight="SemiBold"
					Text="出力フォーマット" />
				<StackPanel Grid.Row="0"
					Grid.Column="1"
					Orientation="Horizontal">
					<RadioButton Margin="0,4,24,4"
						Content="CSV"
						GroupName="OutputFormat"
						IsChecked="{Binding IsCsvFormat, Mode=TwoWay}" />
					<RadioButton Margin="0,4,0,4"
						Content="固定長"
						GroupName="OutputFormat"
						IsChecked="{Binding IsFixedLengthFormat, Mode=TwoWay}" />
				</StackPanel>

				<TextBlock Grid.Row="2"
					Grid.Column="0"
					VerticalAlignment="Center"
					FontSize="15"
					FontWeight="SemiBold"
					Text="出力先" />
				<TextBox x:Name="OutputPathTextBox"
					Grid.Row="2"
					Grid.Column="1"
					materialDesign:HintAssist.Hint="例: C:\hht\hksnds1"
					materialDesign:TextFieldAssist.HasClearButton="True"
					Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" />
			</Grid>
		</materialDesign:Card>

		<Grid Grid.Row="2" Margin="0,20,0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="12" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<Button Grid.Column="1"
				MinWidth="120"
				Command="{Binding CreateDataCommand}"
				Content="データ作成"
				Style="{StaticResource MaterialDesignRaisedButton}" />
			<Button Grid.Column="3"
				MinWidth="100"
				Command="{Binding ExitCommand}"
				Content="戻る"
				IsCancel="True"
				Style="{StaticResource MaterialDesignOutlinedButton}" />
		</Grid>
	</Grid>
</helpers:BaseWindow>
