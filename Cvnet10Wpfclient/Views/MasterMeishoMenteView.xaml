<Window x:Class="Cvnet10Wpfclient.Views.MasterMeishoMenteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{StaticResource MaterialDesignBody}"
        Background="{DynamicResource AppCommonBackgroundBrush}"        
        xmlns:vm="clr-namespace:Cvnet10Wpfclient.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="1100" Height="720" MinWidth="900" MinHeight="600" FontSize="16"
        WindowStartupLocation="CenterOwner"
        Icon="pack://application:,,,/cvnet.ico">

    <Window.DataContext>
        <vm:MasterMeishoMenteViewModel />
    </Window.DataContext>

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="ContentRendered">
            <behaviors:InvokeCommandAction Command="{Binding InitCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <Window.InputBindings>
        <KeyBinding Command="{Binding DoUpdateCommand}" Gesture="F2" />
        <KeyBinding Command="{Binding DoDeleteCommand}" Gesture="F3" />
        <KeyBinding Command="{Binding DoInsertCommand}" Gesture="F4" />
        <KeyBinding Command="{Binding DoListCommand}" Gesture="F5" />
        <KeyBinding Command="{Binding DoSelKubunCommand}" Gesture="F6" />
    </Window.InputBindings>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Width="90" Height="34" Margin="0,0,8,0" Content="閉じる" Command="{Binding ExitCommand}" />
                <Button Width="120" Height="34" Margin="0,0,8,0" Content="再表示 F5" Command="{Binding DoListCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="件数: " VerticalAlignment="Center" />
                <TextBlock Text="{Binding Count}" VerticalAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="  " />
                <TextBlock Text="{Binding Desc0}" VerticalAlignment="Center" />
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="1.1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#FFC6CBD2" BorderThickness="1" CornerRadius="4">
                <DataGrid ItemsSource="{Binding ListData}"
                          SelectedItem="{Binding Current}"
                          IsReadOnly="True"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          AlternationCount="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70" />
                        <DataGridTextColumn Header="区分" Binding="{Binding Kubun}" Width="100" />
                        <DataGridTextColumn Header="コード" Binding="{Binding Code}" Width="110" />
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="順序" Binding="{Binding SortNo}" Width="70" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" BorderBrush="#FFC6CBD2" BorderThickness="1" CornerRadius="4" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="コード" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}" />
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,4,0,8" Foreground="{DynamicResource MaterialDesignBody}"
                             Text="{Binding CurrentEdit.Code, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="名称" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}" />
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,4,0,8" Foreground="{DynamicResource MaterialDesignBody}"
                             Text="{Binding CurrentEdit.Name, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="備考" VerticalAlignment="Top" Margin="0,4,0,0" Foreground="{DynamicResource MaterialDesignBody}" />
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,4,0,8" Height="70" AcceptsReturn="True" TextWrapping="Wrap" Foreground="{DynamicResource MaterialDesignBody}"
                             Text="{Binding CurrentEdit.Memo, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="並び順" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}" />
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,4,0,8" Width="100" HorizontalAlignment="Left" Foreground="{DynamicResource MaterialDesignBody}"
                             Text="{Binding CurrentEdit.SortNo, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="区分ID" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}" />
                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,8">
                        <TextBox Width="80" Margin="0,0,8,0" Text="{Binding CurrentEdit.Id_Kubun}" Foreground="{DynamicResource MaterialDesignBody}" />
                        <Button Width="100" Content="参照 F6" Command="{Binding DoSelKubunCommand}"  />
                    </StackPanel>

                    <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,16,0,16">
                        <Button Width="110" Height="38" Margin="0,0,8,0" Content="更新 F2" Command="{Binding DoUpdateCommand}" Style="{StaticResource MaterialDesignRaisedButton}" />
                        <Button Width="110" Height="38" Margin="0,0,8,0" Content="削除 F3" Command="{Binding DoDeleteCommand}" Style="{StaticResource MaterialDesignPaperButton}" />
                        <Button Width="110" Height="38" Content="登録 F4" Command="{Binding DoInsertCommand}" />
                    </StackPanel>

                    <StackPanel Grid.Row="6" Grid.ColumnSpan="2" VerticalAlignment="Bottom">
                        <Separator Margin="0,0,0,8" />
                        <UniformGrid Columns="2">
                            <StackPanel>
                                <TextBlock Text="作成日時" FontSize="12" Opacity="0.6" Foreground="{DynamicResource MaterialDesignBody}" />
                                <TextBlock Text="{Binding CurrentEdit.VdateC, StringFormat={}{0:yyyy/MM/dd HH:mm}}" Foreground="{DynamicResource MaterialDesignBody}" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="更新日時" FontSize="12" Opacity="0.6" Foreground="{DynamicResource MaterialDesignBody}" />
                                <TextBlock Text="{Binding CurrentEdit.VdateU, StringFormat={}{0:yyyy/MM/dd HH:mm}}" Foreground="{DynamicResource MaterialDesignBody}" />
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>