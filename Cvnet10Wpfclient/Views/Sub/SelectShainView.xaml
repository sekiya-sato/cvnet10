<helpers:BaseWindow x:Class="Cvnet10Wpfclient.Views.Sub.SelectShainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:Cvnet10Wpfclient.Helpers"
        xmlns:vm="clr-namespace:Cvnet10Wpfclient.ViewModels.Sub"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="社員範囲選択"
        Width="620"
        Height="420"
        FontSize="18"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppCommonBackgroundBrush}"
        mc:Ignorable="d">
    <helpers:BaseWindow.DataContext>
        <vm:SelectShainViewModel />
    </helpers:BaseWindow.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,12"
                   Foreground="{DynamicResource MaterialDesignBody}">
            社員一覧の対象範囲を指定してください
        </TextBlock>

        <Border Grid.Row="1"
                Padding="16"
                CornerRadius="6"
                Background="{DynamicResource MaterialDesignPaper}">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Text="社員CD From" />
                <TextBox Grid.Row="0" Grid.Column="1" Height="36" Margin="8,4,0,4"
                         Text="{Binding FromCode, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         FontSize="16" />

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Text="社員CD To" />
                <TextBox Grid.Row="1" Grid.Column="1" Height="36" Margin="8,4,0,4"
                         Text="{Binding ToCode, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         FontSize="16" />

                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,12,0,0"
                           Foreground="Gray" TextWrapping="Wrap">
                    範囲を指定しない場合、すべての社員が対象になります。
                </TextBlock>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="120" Height="38" Margin="0,0,8,0" FontSize="16"
                    Background="Gray" Foreground="White"
                    Command="{Binding CancelCommand}" Content="戻る" IsCancel="True" />
            <Button Width="140" Height="38" FontSize="16"
                    Background="DarkSlateBlue" Foreground="White"
                    Command="{Binding OkCommand}" Content="一覧表示(F5)" IsDefault="True" />
        </StackPanel>
    </Grid>
</helpers:BaseWindow>
