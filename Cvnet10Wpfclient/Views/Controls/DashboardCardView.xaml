<UserControl x:Class="Cvnet10Wpfclient.Views.Controls.DashboardCardView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">
	<Border Margin="8"
		Padding="16"
		d:Background="Gray"
		d:Height="100"
		d:Width="150"
		Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
		BorderBrush="#30FFFF"
		BorderThickness="1"
		CornerRadius="14">
		<StackPanel>
			<TextBlock d:Foreground="White"
				d:Text="TitleSample"
				Foreground="{DynamicResource MaterialDesignBody}"
				Style="{StaticResource MetricLabel}"
				Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />
			<TextBlock d:Foreground="Blue"
				d:Text="Value"
				FontSize="32"
				FontWeight="Bold"
				Foreground="{DynamicResource MaterialDesignBody}"
				Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" />
			<ContentPresenter Content="{Binding CardContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />
		</StackPanel>
	</Border>
</UserControl>
